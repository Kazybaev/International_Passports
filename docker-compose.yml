services:
  passport-mvp:
    build: .
    ports: ["8501:8501"]
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp:size=256m"]
    security_opt: ["no-new-privileges:true"]
    deploy:
      resources:
        limits:
          memory: 2G

